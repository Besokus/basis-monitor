# 验证与基线文档：Basis Monitor

本文档关注“当前项目到底验证了什么、还缺什么、如何重复验证”。  
如果没有可靠数字或仓库内证据，不写成既定事实，而是明确标记为“待验证”或“待补采集”。

另外说明：

- 当前项目是实时监控系统，不是视频工作流系统。
- 因此“视频生成链路”“artifacts 回传”等项对本项目不适用，不纳入指标基线。

## 1. 当前已具备的验证项

### 1.1 构建与关键回归测试可重复执行

- 指标名称 / 验证项名称  
  构建与关键测试回归

- 它验证的目标是什么  
  验证核心代码能编译，且与双行情、联合计算、存储一致性相关的关键测试仍通过。

- 当前是否已有证据  
  有

- 证据来自哪里  
  开发过程中的实际构建与 `ctest` 记录；对应测试目标也在仓库内可见。

- 当前状态  
  已验证

- 关键测试覆盖  
  - `test_basis_monitor_service`
  - `test_basis_result_store`
  - `test_latest_basis_snapshot_store`
  - `test_market_data_health_registry`
  - `test_market_data_session_factory`
  - `test_xtp_market_data_session`

- 为什么它对项目重要  
  这些测试覆盖了当前系统最核心的链路：
  - 双会话创建
  - 联合计算
  - 结果持久化
  - 健康状态拆分
  - XTP 订阅与 tick 入口

- 后续如何补齐  
  建议把“本轮关键测试清单”固化成 CI 或统一回归命令，避免只靠人工记忆。

### 1.2 `CTP` 实盘 connect/login/subscribe/first-tick 已现场验证

- 指标名称 / 验证项名称  
  `CTP` 实盘行情接入链路

- 它验证的目标是什么  
  验证正式版 CTP 行情 API 和采集库不仅能编译，而且能在现场：
  - 连接前置
  - 登录成功
  - 订阅成功
  - 收到首笔实盘行情

- 当前是否已有证据  
  有，但主要来自开发过程日志摘录，尚未统一归档到仓库文档附件

- 证据来自哪里  
  已记录过的现场日志关键信号：
  - `MdApiVersion`
  - `DataCollectVersion`
  - `OnFrontConnected`
  - `OnRspUserLogin`
  - `OnRspSubMarketData`
  - `MARKET_DATA_OK`

- 当前状态  
  已验证

- 为什么它对项目重要  
  这说明 `CTP` 这条主链已经从“能编译”提升到了“能在目标环境真实收数”。

- 后续如何补齐  
  建议把一次成功现场日志样本脱敏后归档到 `docs/` 或 `logs/samples/`。

### 1.3 运行时订阅列表与监控池的一致性

- 指标名称 / 验证项名称  
  Top4 监控池与订阅派生一致性

- 它验证的目标是什么  
  验证当天监控对象与行情订阅对象一致，不会出现：
  - 监控池是一套
  - 期货订阅另一套
  - 指数订阅再另一套

- 当前是否已有证据  
  有

- 证据来自哪里  
  启动日志会打印：
  - `CtpInstrumentIDs = ...`
  - `XtpIndexInstrumentIDs = ...`
  代码逻辑在 [app/main.cpp](c:\Users\yp636\Documents\workdir\6.7.11apidemo\.worktrees\basis-monitor\basis_monitor\app\main.cpp)

- 当前状态  
  已验证（代码与日志路径具备）

- 为什么它对项目重要  
  这是双链路联合计算的前提，直接关系到结果口径是否统一。

- 后续如何补齐  
  建议在 README 中补一段“如何用日志确认订阅派生正确”。

### 1.4 预编译部署链路可重复执行

- 指标名称 / 验证项名称  
  中泰预编译部署能力

- 它验证的目标是什么  
  验证在“不能上传源代码”的约束下，仍能：
  - 上传预编译可执行文件
  - 上传 CTP/XTP 运行库
  - 在中泰后台启动

- 当前是否已有证据  
  有

- 证据来自哪里  
  脚本与目录结构：
  - [scripts/push_prebuilt_runtime_to_zhongtai.sh](c:\Users\yp636\Documents\workdir\6.7.11apidemo\.worktrees\basis-monitor\basis_monitor\scripts\push_prebuilt_runtime_to_zhongtai.sh)
  - [start_prebuilt.sh](c:\Users\yp636\Documents\workdir\6.7.11apidemo\.worktrees\basis-monitor\basis_monitor\start_prebuilt.sh)

- 当前状态  
  已验证（脚本存在且已用于现场）

- 为什么它对项目重要  
  这是系统在目标环境能否真正落地运行的核心保障。

- 后续如何补齐  
  建议补一份“中泰运行包清单校验表”，记录每次上线实际上传了哪些文件。

### 1.5 工作目录与相对路径行为可控

- 指标名称 / 验证项名称  
  cwd 正确性

- 它验证的目标是什么  
  验证程序运行时能找到：
  - `config/`
  - `data/`
  - `vendor/`
  - `logs/`

- 当前是否已有证据  
  有

- 证据来自哪里  
  - `run.sh` 会从项目目录构建并复制二进制到 `bin/`  
  - `start.sh` 与 `start_prebuilt.sh` 都显式 `cd "$PROJECT_DIR"` 后再启动
  - 历史上已经出现过“从错误 cwd 启动导致找不到 `data/all_instruments/Future`”的实际问题，并已明确定位

- 当前状态  
  已验证

- 为什么它对项目重要  
  这类问题不会直接体现在编译阶段，但会导致目标机启动后立即失败，是非常常见的线上问题。

- 后续如何补齐  
  建议在运行脚本中继续保留对关键目录的存在性检查。

### 1.6 本地清洗后上传 staging 的数据同步链路已存在

- 指标名称 / 验证项名称  
  本地参考数据清洗与 SFTP 上传

- 它验证的目标是什么  
  验证本地服务器可以：
  - 从原始 CSV 目录筛出当天监控合约及其对应指数
  - 上传到中泰 staging

- 当前是否已有证据  
  有

- 证据来自哪里  
  - [scripts/push_reference_data_to_zhongtai.sh](c:\Users\yp636\Documents\workdir\6.7.11apidemo\.worktrees\basis-monitor\basis_monitor\scripts\push_reference_data_to_zhongtai.sh)
  - `prepare_reference_subset` 的构建与调用逻辑

- 当前状态  
  部分验证

- 为什么它对项目重要  
  这决定了中泰运行时到底拿到的是全量市场数据还是“当前监控范围内的数据子集”。

- 后续如何补齐  
  建议补一份对照验证：
  - 上传前后 CSV 行数
  - 中泰 staging 中实际存在的 4 类目录和文件

## 2. 当前缺失但重要的基线项

### 2.1 `XTP` 连续收数稳定性基线

- 指标名称 / 验证项名称  
  `XTP` 首包与连续 `INDEX_TICK`

- 它验证的目标是什么  
  验证 `XTP` 在真实交易时段内能持续收到目标指数 tick，而不仅是完成登录或启动。

- 当前是否已有证据  
  部分有

- 证据来自哪里  
  - 代码已支持
  - 启动日志能打印 `XtpIndexInstrumentIDs`
  - 现场曾看到 `MARKET_DATA_WAIT_TIMEOUT`，说明至少进入过等待首包阶段

- 当前状态  
  待验证

- 为什么它对项目重要  
  这是联合计算是否真正可用的关键前提。

- 后续如何补齐  
  - 记录首包时间
  - 记录连续 `INDEX_TICK` 样本
  - 记录断流与恢复情况

### 2.2 双链路同时启用时的真实运行基线

- 指标名称 / 验证项名称  
  `CTP + XTP` 同时运行基线

- 它验证的目标是什么  
  验证双链路同时开启时：
  - `CTP` 能继续正常收数
  - `XTP` 能正常收数
  - 联合计算能够产生结果

- 当前是否已有证据  
  部分有

- 证据来自哪里  
  代码与日志路径已经具备，但缺少完整交易时段现场记录

- 当前状态  
  待验证

- 为什么它对项目重要  
  这是当前架构目标是否真正落地的最终基线。

- 后续如何补齐  
  建议补一轮交易时段联调 checklist，并归档结果。

### 2.3 报表可信度基线

- 指标名称 / 验证项名称  
  报表是否真正建立在双链路新鲜数据之上

- 它验证的目标是什么  
  验证报表里的结果对应的是：
  - 新鲜 future 数据
  - 新鲜 index 数据

- 当前是否已有证据  
  不充分

- 证据来自哪里  
  当前后台逻辑已经限制 stale index 不参与计算，但报表头部没有体现 index 通道状态。

- 当前状态  
  部分验证

- 为什么它对项目重要  
  直接影响“报表中的数能不能被业务方信任”。

- 后续如何补齐  
  不一定要在报表中打印双通道字段，但至少要在后台保留可追溯审计信息。

### 2.4 `CTP` 启动卡住场景的超时基线

- 指标名称 / 验证项名称  
  `CTP` 连接 / 登录 / 订阅超时

- 它验证的目标是什么  
  验证当 `CTP` 前置异常时，系统能在预期时间内退出或打出明确故障信息。

- 当前是否已有证据  
  没有

- 证据来自哪里  
  代码当前使用 `INFINITE` 等待，没有形成可重复超时基线。

- 当前状态  
  缺失

- 为什么它对项目重要  
  这会影响线上问题定位和守护策略。

- 后续如何补齐  
  后续若引入分阶段启动超时，应把超时值和失败分类纳入基线。

## 3. 建议后续补采集的指标

### 3.1 行情侧
- `CTP` 首包时间
- `XTP` 首包时间
- 每分钟 `Future` / `Index` tick 数
- stale 次数与恢复次数

### 3.2 计算侧
- 每分钟联合计算成功次数
- `WAITING_FOR_LIVE_INDEX` 次数
- `STALE_LIVE_INDEX` 次数
- 每个合约的结果更新时间间隔

### 3.3 告警侧
- 进入负值告警次数
- 重复提醒次数
- 因缺失实时指数被跳过的潜在告警次数

### 3.4 部署与运维侧
- 本地上传耗时
- 中泰启动耗时
- 回拉输出耗时
- 预编译包大小和运行库清单

## 4. 指标与项目价值的关联

### 4.1 为什么要关注“服务是否正常运行”
- 因为这个项目的价值不在于离线算一次结果，而在于持续接收行情、持续监控、持续输出告警

### 4.2 为什么要关注“cwd、路径、动态库”
- 这是受限部署环境中最常见的真实故障源
- 也是“本地能跑、目标机跑不起来”的主要原因

### 4.3 为什么要关注“首包”和“连续收数”
- 首包验证的是能不能接通
- 连续收数验证的是能不能真正工作
- 两者都重要，不能用一个代替另一个

### 4.4 为什么要关注“后台可追溯性”
- 当前系统已经把准确性门槛编码进计算逻辑
- 但如果没有后台状态和基线，后续很难回答：
  - 为什么这段时间没有结果
  - 为什么这次没告警
  - 为什么某份报表可信

### 4.5 为什么当前不写“视频工作流 / artifacts 回传”
- 因为这些并不是本项目的功能目标
- 知识沉淀必须尊重项目边界，不能为了凑模板而引入无关指标
