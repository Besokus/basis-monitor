# Basis Monitor 项目总览

这份文档是 `Basis Monitor` 的“总认知入口”。它不替代架构决策、面试故事或基线文档，而是把这些材料串成一条完整主线，方便你做项目回顾、面试答题和后续复盘。

相关细节可继续下钻：
- [architecture_decisions.md](architecture_decisions.md)
- [interview_stories.md](interview_stories.md)
- [metrics_baseline.md](metrics_baseline.md)
- [resume_highlights.md](resume_highlights.md)
- [docs/context_handoff_2026-04-13.md](docs/context_handoff_2026-04-13.md)

## 1. 项目一句话概括

`Basis Monitor` 是一个面向股指期货盘中监控的实时系统。它在中泰受限环境中运行，持续接收 `CTP` 期货行情和 `XTP` 指数行情，动态计算年化基差，并在触发条件下输出告警、日内报表和可追溯的运行结果。

如果要用面试里的一句话讲清楚，可以概括为：

> 我做的是一套“实时双行情接入 + 动态监控池派生 + 联合计算 + 告警/报表输出 + 双机定时编排”的股指期货盘中监控系统，核心目标是让中泰环境里的监控结果既能实时、又能稳定、还可审计。

## 2. 业务背景与目标

这个项目的业务目标不是单纯“接行情”，而是把股指期货盘中的监控从静态、单源、离线，推进到一个更贴近真实交易节奏的实时系统。

核心诉求包括：
- 同时拿到期货和指数两类实盘行情，避免只看单边数据。
- 在盘中快速识别年化基差转负、持续转负、以及需要提示关注的情况。
- 对关键结果做落盘，方便复盘、审计和后续问题定位。
- 因为中泰服务器不能直连外网，所以要把告警和报表转发放到本地服务器执行。

从业务上看，这个项目解决的是三个问题：
1. **看得见**：实时看到当前监控标的的基差和状态。
2. **讲得清**：告警为什么触发、报表为什么这样排、每个结论基于什么数据。
3. **跑得稳**：在受限部署环境里也能稳定运行，且能通过定时任务自动维护数据和服务。

## 3. 总体架构

当前系统采用双机分工，边界很清晰。

### 3.1 本地服务器负责什么

本地服务器的职责更偏“运维编排和对外转发”：
- 读取本地参考数据源并筛出当天需要监控的合约集合。
- 通过 `SFTP` 上传精简后的 staging 数据到中泰。
- 轮询拉取中泰服务器上的 `alert_events.csv`、`basis_results.csv` 和报表 txt。
- 解析并通过企业微信机器人转发告警与日内报表。
- 维护本地 relay 状态，避免重复发送。

### 3.2 中泰服务器负责什么

中泰服务器是实时运行的业务主机：
- 接收 `CTP` 期货行情。
- 接收 `XTP` 指数行情。
- 进行联动计算，产出基差结果、告警快照和日内报表文本。
- 将结果写到本地目录，供本地 relay 拉取。

### 3.3 主数据流

可以按下面这条链路理解：

```text
本地参考数据 -> 精简 staging -> 中泰 basis_monitor 运行
-> 实时行情输入(CPT + XTP) -> 联合计算 -> alert_events / basis_results / reports
-> 本地 relay 拉取 -> 企业微信机器人发送
```

### 3.4 时间编排

当前日常运行逻辑大致是：
- 工作日 `09:30`：本地上传参考数据并启动 relay；中泰启动 `basis_monitor`。
- 工作日盘中：本地每隔 10 秒拉取一次中泰输出。
- 工作日 `11:30` 和 `15:00`：中泰生成日内报表文本，本地按时转发到企业微信。
- 工作日 `15:05`：停止中泰和本地 relay。

这个编排的意义在于把“数据准备、实时运行、结果转发、日终清理”全部纳入自动化，不再依赖手工操作。

## 4. 模块划分

### 4.1 行情接入层

行情接入层的核心是双源输入：
- `CTP` 负责期货实盘行情。
- `XTP` 负责指数实盘行情。

对应实现上，项目把两类行情在运行时分流，不再把所有行情都当成同一种 tick 处理。这是后续联合计算可扩展的前提。

### 4.2 监控计算层

监控计算层负责把两类行情转成监控语义：
- 维护实时指数缓存。
- 在期货 tick 到来时触发联动计算。
- 计算基差、年化基差、剩余天数。
- 判断是否进入负值区间以及是否重复提醒。

这一层是项目的业务核心，因为它决定了系统最终看见的不是“行情”，而是“可行动的监控结论”。

### 4.3 存储层

存储层主要负责三类内容：
- `basis_results.csv`：每次有效联动计算结果。
- `alert_events.csv`：告警事件快照。
- `reports/*.txt`：日内报表文本。

这一层的价值在于把“实时计算结果”变成“可追溯数据”，方便排障、审计和面试时讲清楚系统到底产出了什么。

### 4.4 报表与通知层

这个层做两件事：
- 把中泰生成的报表文本整理成可读的企业微信消息。
- 把告警快照整理成带字段的 markdown 卡片。

现在的报表已经不是纯图片了，而是可解释的文本/markdown 格式，更适合复盘和问题追踪。

### 4.5 运维编排层

运维编排层把整个链路串起来：
- 本地上传参考数据。
- 中泰启动/停止。
- 本地 relay 启停。
- 定时拉取与定时清理。

这部分看起来像脚本，但实际上是系统稳定性的关键，因为它决定了数据是否按时准备好、服务是否按时启停、状态是否按天切换。

## 5. 关键技术亮点

### 5.1 双行情分工

项目不是简单把两个行情接口都接上，而是明确了职责边界：
- `CTP` 专注期货。
- `XTP` 专注指数。
- 主监听器把两者合流成同一套监控输入。

这让系统后续的计算、告警、报表逻辑都能围绕“同一批监控对象”来设计，而不是围着接口本身打转。

### 5.2 实时联动计算

系统不是拿“昨收指数 + 实时期货”做单边近似，而是让实时指数进入计算链路。
这让告警和报表更接近盘中的真实状态，也更能支撑业务解释。

### 5.3 可追溯的告警快照

告警不是只发一条“负了”，而是把：
- 合约
- 指数
- 年化基差
- 基差
- 指数价
- 期货价
- 剩余天数
- 时间

一起落盘，形成一条完整快照。  
这个设计特别适合面试，因为它体现的是“业务可解释性”，而不是只会做一个阈值提醒。

### 5.4 双机受限部署

中泰服务器不能直连外网，所以系统把职责分成两段：
- 本地负责编排、预处理、拉取、转发。
- 中泰负责实时运行和结果输出。

这让系统在受限环境里仍然能闭环运行，属于很典型的“受限部署条件下的工程化处理”。

### 5.5 面向故障的可观察性

系统里保留了比较多的运行痕迹：
- 启动日志。
- 行情接收日志。
- 结果文件。
- 告警文件。
- relay 状态文件。

这意味着出现问题时，能快速回答：
- 是没接到行情？
- 是接到了但没算出来？
- 是算出来了但没转发？
- 还是转发了但被去重/时点挡住？

这种可观察性在面试里非常加分，因为它体现的是完整工程思维。

## 6. 业务流程理解

### 6.1 盘中实时链路

盘中时，系统的真实工作流程是：
1. 中泰拉到最新参考数据后启动。
2. `CTP` 和 `XTP` 分别接入对应行情。
3. `XTP` 更新指数缓存。
4. `CTP` 的期货 tick 触发联动计算。
5. 计算结果写入结果文件与告警文件。
6. 本地 relay 拉取并转发企业微信。

### 6.2 日内报表链路

报表链路是另一条平行输出：
1. 中泰在 `11:30` 和 `15:00` 生成报表文本。
2. 本地定时拉取报表。
3. 本地把报表重排成更适合阅读的 markdown。
4. 再通过企业微信机器人发送。

### 6.3 为什么强调“当天”

这个项目里很多问题都来自跨日状态污染，比如：
- 历史告警被重复发送。
- 旧报表被重新发一次。
- 本地状态文件和远端文件不同步。

所以“按交易日切分状态”不是一个小优化，而是系统正确性的基础之一。

## 7. 项目中已经解决的关键问题

### 7.1 从单行情到双行情

解决了 `CTP` 和 `XTP` 二选一的局限，让系统真正能同时拿到期货和指数，形成联动监控闭环。

### 7.2 从静态基线到实时联动

把年化基差的输入从“静态参考值”升级为“实时期货 + 实时指数”，监控结果更贴近盘中状态。

### 7.3 从单次计算到可追溯输出

把计算结果写入结果文件和告警快照，让每条判断都可回放、可解释、可审计。

### 7.4 从手工运维到双机自动化

本地和中泰的责任边界已经清晰：
- 本地负责上传、拉取、转发、清理。
- 中泰负责实时计算和输出。

这比“直接在目标机器上手工跑脚本”稳定得多，也更符合生产环境的要求。

## 8. 当前项目的现实限制

这部分很重要，因为面试中经常会追问边界。

### 8.1 仍然是受限部署

中泰不能直连外网，所以本地 relay 和中泰运行是两套链路，不是一个机器内的闭环。

### 8.2 `XTP` 稳定性仍需要交易时段验证

代码层面已经打通，但是否在真实交易时段持续稳定收数，还需要按交易日继续验证。

### 8.3 报表和告警都是“状态驱动”

这意味着如果时点、文件名、状态文件、去重逻辑有偏差，就会直接影响输出行为。  
所以这不是“写完代码就结束”的项目，而是“持续观察、持续校正”的项目。

### 8.4 运行环境对编译产物敏感

本地定时任务和 `prepare_reference_subset` 的编译/运行环境绑定比较紧，曾经出现过 `GLIBCXX`、`GLIBC`、`CMakeCache` 等问题。  
这说明项目的工程性在增强，但也提醒你：交付时必须把环境和可执行文件一起看。

## 9. STAR 面试讲法

下面这几段是你面试时可以直接复述的主线。

### 9.1 STAR 1：双行情架构

- `S`：原系统更偏单行情接入，但业务已经要求同时监控期货和指数。
- `T`：要把 `CTP` 和 `XTP` 合到同一套监控里，同时尽量不破坏原来的稳定链路。
- `A`：我把行情职责拆开，`CTP` 负责期货、`XTP` 负责指数，并在监听器和计算层合流。
- `R`：系统从单源模式升级为双源联动，后续的联动计算、告警和报表都建立在统一输入之上。

### 9.2 STAR 2：实时联动计算

- `S`：早期的计算里，期货是实时的，但指数还偏静态。
- `T`：要把监控从“静态口径”升级成“盘中口径”。
- `A`：我引入实时指数缓存，让期货 tick 触发联动计算，同时保留状态门槛避免 stale 数据污染结果。
- `R`：系统最终能在盘中输出更贴近真实市场状态的基差结果和告警。

### 9.3 STAR 3：受限部署下的双机编排

- `S`：中泰服务器不能直连外网，不能把所有逻辑都放在目标机上。
- `T`：要实现可自动启动、可自动清理、可转发结果的双机闭环。
- `A`：我拆成本地编排 + 中泰运行两部分，本地负责上传、拉取、转发，中泰负责实时计算和输出。
- `R`：系统实现了在受限环境下的自动化运行闭环，并且能按工作日自动启停。

### 9.4 STAR 4：可观察性和排障

- `S`：实际运行时，最难的是定位问题是发生在行情、计算、转发还是运维脚本。
- `T`：要让系统出现问题时能快速定位。
- `A`：我补了运行日志、结果文件、告警快照和 relay 状态文件，并把日内报表和告警都做成可追溯输出。
- `R`：现在可以根据日志和落盘文件快速判断问题点，而不是只看“有没有发出去”。

## 10. 面试高频问题怎么答

### 10.1 为什么要做双行情？

因为业务目标本身就要求把期货和指数放到同一套基差监控里，单一行情只能得到部分结论，无法支撑盘中监控的完整业务口径。

### 10.2 为什么不是简单把两个接口都接上？

因为“接上接口”不等于“让业务链路真的用起来”。我把重点放在了：
- tick 语义统一
- 运行时订阅派生
- 联动计算合流
- 状态和去重闭环

### 10.3 为什么告警要落盘快照？

因为告警不只是发消息，还要能复盘：当时用的是什么合约、什么指数、什么价、为什么触发。  
没有快照，后面很难定位误报、漏报或时序问题。

### 10.4 为什么要把本地和中泰拆开？

因为中泰不允许直连外网，而企业微信转发必须走本地网络。所以必须把实时运行和外部转发拆开，形成可控的双机结构。

### 10.5 你最难的问题是什么？

可以回答为：
> 最难的不是某一个 API 调用，而是把行情接入、联动计算、告警落盘、报表输出和双机编排串成一个可长期运行的系统，并且在受限部署环境下保持可观察性和可回滚性。

## 11. 你可以直接背的项目总结

如果面试官让你用 1 分钟概括项目，可以这样讲：

> 我做的是一套股指期货盘中基差监控系统。这个系统在中泰受限环境里运行，实时接入 CTP 期货行情和 XTP 指数行情，把双行情在运行时合流做联动计算，输出年化基差、告警快照和日内报表。本地服务器负责参考数据准备、SFTP 同步、结果拉取和企业微信转发，中泰服务器负责实时计算和结果落盘。整个项目的关键点不只是接入行情，而是把双行情分工、实时联动、状态去重、双机编排和可追溯输出一起打通，确保系统在生产环境里能稳定跑、能解释、能排障。

## 12. 你接下来复盘时应该优先看的材料

如果你要进一步准备面试，我建议按这个顺序看：
1. `summary.md`
2. `architecture_decisions.md`
3. `interview_stories.md`
4. `metrics_baseline.md`
5. `resume_highlights.md`

这样能先形成整体认知，再往细节下钻，不容易在面试时只记得零散命令和零散脚本，而讲不清主线。

## 13. 当前可继续补强的方向

如果后续还有时间，这份总综述还可以继续补三类内容：
- 更完整的指标基线，比如某些交易时段的稳定运行时长、告警触发量、报表生成成功率。
- 更精确的故障回放，比如某一次定时任务失败、某一次报表错发、某一次 `XTP` 断流。
- 更完整的面试问答卡片，比如“为什么这样设计而不是那样设计”的标准答案版。

这份文档的目标不是一次写完所有知识，而是给你一个足够稳定的主叙事入口。后续任何新问题，都可以先回到这里，再去四个子文档补细节。
