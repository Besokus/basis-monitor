# 简历亮点候选池：Basis Monitor

本文档不是最终简历，而是“简历亮点候选池”。每个条目都尽量回答：

- 背景问题是什么
- 我具体做了什么
- 工程价值在哪里
- 当前证据是否充分
- 适合如何写成简历 bullet
- 面试时可能会被追问什么

## A. 强候选亮点（当前证据较充分）

### 亮点 1：把单行情源监控改造成 `CTP + XTP` 双链路联合监控架构

- 背景 / 问题  
  原系统更接近单行情源模式，`CTP` 与 `XTP` 只能二选一；但真实业务需要 `CTP` 提供期货实盘价、`XTP` 提供指数实盘价，并在同一套监控链中联合使用。

- 我的动作 / 贡献  
  设计并落地了双会话架构，补齐：
  - 双会话工厂
  - Tick 来源与资产类型语义
  - `CTP`/`XTP` 运行时订阅列表派生
  - 主流程监听器分流

- 技术价值 / 工程价值  
  - 把行情接入从“互斥配置”升级为“职责分离 + 运行时合流”
  - 为后续联合计算、健康检查拆分、报表可信度控制打下基础

- 可量化结果  
  - 运行时固定监控 3 个业务分组：`hs300 / zz500 / zz1000`
  - 每组最多 4 个合约，共 12 个期货监控标的
  - `XTP` 运行时指数订阅由同一批监控合约自动派生，避免双配置漂移

- 适合写成的简历表达方式  
  版本 1：设计并实现股指期货监控系统的 `CTP + XTP` 双行情接入架构，将原有单源模式升级为“期货/指数分链接入、运行时合流计算”的双链路模型。  
  版本 2：主导期货监控系统行情层重构，支持 `CTP` 订阅 Top4 期货合约、`XTP` 自动订阅对应指数，打通双行情统一监听与计算入口。

- 面试时可能的追问点  
  - 为什么不继续用单行情源
  - 为什么让 `CTP` 触发计算，而不是任一 tick 都重算
  - 双链路一致性怎么保证

### 亮点 2：把联合计算从“昨收指数”升级成“实时期货 + 实时指数”

- 背景 / 问题  
  初始实现里，期货价是实时的，但指数价来自参考数据中的昨收，无法满足更高准确性的盘中告警要求。

- 我的动作 / 贡献  
  - 在 `BasisMonitorService` 中引入实时指数缓存
  - 让 `XTP` 指数 tick 先更新缓存
  - 让 `CTP` 期货 tick 触发联合计算
  - 统一将“本次实际使用的指数价”写入结果落盘和快照存储

- 技术价值 / 工程价值  
  - 真正把双行情链路拉通到业务计算层，而不仅是“两个接口都能连”
  - 避免“代码看起来双链路，结果仍按昨收算”的假闭环

- 可量化结果  
  - 关键回归测试覆盖 6 项联合计算相关路径并通过：
    - `test_basis_monitor_service`
    - `test_basis_result_store`
    - `test_latest_basis_snapshot_store`
    - `test_market_data_health_registry`
    - `test_market_data_session_factory`
    - `test_xtp_market_data_session`

- 适合写成的简历表达方式  
  版本 1：推动年化基差计算从“实时期货 + 指数昨收”升级为“`CTP` 实时期货 + `XTP` 实时指数”，并同步改造落盘与快照链路，保证结果、报表与告警口径一致。  
  版本 2：完成双行情联合计算链路落地，引入实时指数缓存与运行时价格持久化，消除监控结果与数据源不一致的问题。

- 面试时可能的追问点  
  - 为什么不是 `XTP` tick 触发计算
  - 如何处理指数断流或首包未到
  - 为什么落盘也要写“实际使用的指数价”

### 亮点 3：为年化基差告警引入“准确性优先”的实时性门槛

- 背景 / 问题  
  实时指数接入后，系统必须在“启动时效性”和“告警准确性”之间做选择。

- 我的动作 / 贡献  
  明确并落地了决策：
  - 程序可及时启动
  - 但只要 `XTP` 已启用，计算和告警必须依赖新鲜实时指数价
  - `WAITING_FOR_LIVE_INDEX` / `STALE_LIVE_INDEX` 时暂停对应合约的计算与告警

- 技术价值 / 工程价值  
  - 把业务准确性要求显式编码进系统行为，而不是停留在文档或人工约定
  - 避免“系统看起来还在跑，但告警依据其实已经过期”的隐性错误

- 可量化结果  
  - 新增 2 类显式跳过状态：`WAITING_FOR_LIVE_INDEX`、`STALE_LIVE_INDEX`
  - 通过单测验证了：
    - 未到指数首包时不计算
    - 指数 stale 后不计算
    - 单链路 fallback 仍可用

- 适合写成的简历表达方式  
  版本 1：针对盘中告警准确性要求，设计并实现“及时启动、严格告警”策略，引入实时指数新鲜度门槛，杜绝使用过期指数价继续触发告警。  
  版本 2：将业务规则转化为运行时控制逻辑，显式区分等待指数首包、指数断流 stale、正常联合计算三种状态，提升监控结果可信度。

- 面试时可能的追问点  
  - 为什么不直接等双首包再启动
  - 为什么不使用昨收作为兜底
  - 如果业务更看重连续输出怎么办

### 亮点 4：在受限环境下构建“本地编译 + SFTP + 中泰预编译运行”的交付链

- 背景 / 问题  
  中泰服务器受限，只允许上传编译好的可执行文件和数据文件，不允许上传源代码。

- 我的动作 / 贡献  
  - 明确双服务器职责
  - 建立本地预处理参考数据、SFTP 上传、预编译 runtime 上传、结果回拉的脚本链
  - 规范中泰目录结构与后台启动方式

- 技术价值 / 工程价值  
  - 把“受限生产环境”转成可重复执行的交付流程
  - 降低现场部署对人工操作和口口相传经验的依赖

- 可量化结果  
  - 运行包上传脚本可同时携带：
    - `basis_monitor` 可执行文件
    - 正式版 CTP 运行库
    - 看穿式采集库
    - XTP 行情库
  - 参考数据同步链路支持 4 类 CSV staging 的一键预处理与上传

- 适合写成的简历表达方式  
  版本 1：在“目标机不可上传源码”的约束下，搭建本地编译、SFTP 同步、目标机预编译运行的交付流程，固化运行库与脚本，降低现场部署风险。  
  版本 2：面向受限交易环境，设计双服务器协作模式：本地负责构建与数据清洗，中泰仅运行预编译产物与接收 staging 数据，实现可回滚、可审计的上线链路。

- 面试时可能的追问点  
  - 为什么不在目标机编译
  - 如何确保运行时动态库正确
  - 数据和程序为什么分两条同步链

### 亮点 5：完成正式版 CTP Linux API 与看穿式采集库接入，并打通实盘首包

- 背景 / 问题  
  旧测试库和正式实盘库并存时，容易出现握手失败、版本不匹配、动态库误加载等问题。

- 我的动作 / 贡献  
  - 切换到正式版 Linux `MdApi` 与 `LinuxDataCollect.so`
  - 固化项目内 vendor 路径
  - 更新构建、运行、预编译上传脚本
  - 通过现场日志完成 connect/login/subscribe/first-tick 验证

- 技术价值 / 工程价值  
  - 把“能编译”提升到“能在实盘前置上真正登录并收数”
  - 降低运行时加载错库的风险

- 可量化结果  
  - 现场已看到：
    - `OnFrontConnected`
    - `OnRspUserLogin`
    - `OnRspSubMarketData`
    - `MARKET_DATA_OK`
  - 相关正式版运行时信息会写日志：
    - `MdApiVersion`
    - `DataCollectVersion`

- 适合写成的简历表达方式  
  版本 1：完成正式版 CTP Linux 行情 API 与看穿式采集库接入，解决测试库/正式库并存下的握手与运行时依赖问题，并在现场验证 connect/login/subscribe/first-tick 全链路。  
  版本 2：主导 CTP 实盘接入升级，统一正式版 runtime vendor 路径，修复动态库加载与部署路径问题，打通实时行情首包。

- 面试时可能的追问点  
  - 如何判断是网络问题还是库版本问题
  - 为什么要把正式版 so 固化到项目里
  - 如何验证目标机确实加载了正确库

## B. 待补证据亮点（方向好，但还需要更多验证）

### 亮点 1：`XTP` 实盘指数连续收数稳定性

- 背景 / 问题  
  代码层订阅、缓存、联合计算入口都已具备，但“连续交易时段下 `INDEX_TICK` 的稳定性”还未形成完整归档证据。

- 我的动作 / 贡献  
  已完成：
  - 指数订阅自动派生
  - `XTP` 会话启动、首包等待逻辑
  - `INDEX_TICK` 日志链路

- 技术价值 / 工程价值  
  这是联合计算是否真正闭环的关键证据。

- 可量化结果  
  待补证据：需要连续交易时段日志、首包时间、断流恢复记录。

- 适合写成的简历表达方式  
  当前不建议直接写成“已稳定打通 `XTP` 实盘收数”。  
  更稳妥的表达是：已完成 `XTP` 接入链路与联合计算入口，现场连续收数验证待补充。

- 面试时可能的追问点  
  - 是否已经在真实交易时段稳定运行
  - 首包和断流恢复情况怎么样

### 亮点 2：双链路联合计算的现场告警回归

- 背景 / 问题  
  联合计算代码已改完，但“在真实行情波动下告警频率和报表口径是否符合预期”还缺交易时段回归证据。

- 我的动作 / 贡献  
  已完成后台门槛控制和回归测试，但现场业务验证未完。

- 技术价值 / 工程价值  
  这会直接影响是否可以把“准确性优先”策略写成高置信度产出。

- 可量化结果  
  待补证据：需要真实交易时段的：
  - 告警数量
  - 跳过计算次数
  - stale 发生频次

- 适合写成的简历表达方式  
  当前建议写成“完成联合计算与准确性控制设计并落地代码”，不要写成“已在生产稳定运行”。

- 面试时可能的追问点  
  - 如何验证告警没有误报
  - 是否出现过 stale 期间错误告警

### 亮点 3：端到端双服务器运维闭环

- 背景 / 问题  
  脚本链路已经齐了，但“长期稳定运行 + 定时任务自动化 + 清理策略”还没完全做完。

- 我的动作 / 贡献  
  已落地：
  - 本地清洗上传
  - 预编译运行包上传
  - 输出结果回拉
  - 中泰后台运行

- 可量化结果  
  待补证据：需要自动化运行时长、失败重试、日切清理等数据。

- 适合写成的简历表达方式  
  当前可以写“搭建了交付和运行链路”，不建议写“已完成全自动运维体系”。

## C. 暂不建议写入简历的点（原因说明）

### 1. “XTP 实盘稳定接收已完全验证”
- 原因：当前只有代码链路和部分现场启动证据，缺连续交易时段稳定日志

### 2. “企业微信告警链路已完全稳定”
- 原因：历史记录里出现过无效 webhook，当前更适合作为辅助链路，不宜写成已稳定结果

### 3. “系统性能显著优化 / 延迟显著下降”
- 原因：没有成体系的性能基线和前后对比数字

### 4. “生产级高可用体系已完成”
- 原因：目前还缺自动化守护、显式降级模式、双通道报表状态表达等收口项

## D. 后续可补强方向（为了未来简历更强，建议补哪些数据/验证）

### 1. 补连续交易时段的 `XTP` 收数证据
- 记录首包时间、连续 `INDEX_TICK` 数量、断流恢复情况

### 2. 补联合计算现场样本
- 记录若干真实时点的：
  - 期货价
  - 指数价
  - basis
  - annual_rate
  - 是否触发告警

### 3. 补端到端运维基线
- 本地上传耗时
- 中泰启动耗时
- 结果回拉耗时
- 一天内成功运行次数 / 失败次数

### 4. 补稳定性与故障定位数据
- stale 次数
- `WAITING_FOR_LIVE_INDEX` 次数
- `STALE_LIVE_INDEX` 次数
- 双链路启动失败的分类统计

### 5. 补“改造前后差异”证据
- 单行情模式与双行情模式的能力差异
- 昨收指数价与实时指数价参与计算的语义差异
